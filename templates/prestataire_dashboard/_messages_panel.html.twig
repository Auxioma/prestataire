<ul class="nav nav-tabs border-bottom gap-2 mb-4" id="messagesSubTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <button
            class="nav-link active"
            id="messages-all-tab"
            data-tab-level="sub"
            data-bs-toggle="tab"
            data-bs-target="#messages-all-panel"
            type="button"
            role="tab"
            aria-controls="messages-all-panel"
            aria-selected="true"
        >
            Tous les messages
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button
            class="nav-link"
            id="messages-inbox-tab"
            data-tab-level="sub"
            data-bs-toggle="tab"
            data-bs-target="#messages-inbox-panel"
            type="button"
            role="tab"
            aria-controls="messages-inbox-panel"
            aria-selected="false"
        >
            Messages reçus
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button
            class="nav-link"
            id="messages-sent-tab"
            data-tab-level="sub"
            data-bs-toggle="tab"
            data-bs-target="#messages-sent-panel"
            type="button"
            role="tab"
            aria-controls="messages-sent-panel"
            aria-selected="false"
        >
            Messages envoyés
        </button>
    </li>
</ul>

<div class="tab-content" id="messagesSubTabsContent">
    <div
        class="tab-pane fade show active"
        id="messages-all-panel"
        role="tabpanel"
        aria-labelledby="messages-all-tab"
        tabindex="0"
    >
        <div class="card tm-card p-4">
            <h4 class="tm-card-title mb-3">Tous les messages</h4>
            <p class="text-muted mb-0">Cette tab regroupera l’ensemble des conversations du prestataire.</p>
        </div>
    </div>

    <div
        class="tab-pane fade"
        id="messages-inbox-panel"
        role="tabpanel"
        aria-labelledby="messages-inbox-tab"
        tabindex="0"
    >
        <div class="card tm-card p-4">
            <h4 class="tm-card-title mb-3">Messages reçus</h4>
            <p class="text-muted mb-0">Cette tab affichera les messages entrants à consulter et traiter.</p>
        </div>
    </div>

    <div
        class="tab-pane fade"
        id="messages-sent-panel"
        role="tabpanel"
        aria-labelledby="messages-sent-tab"
        tabindex="0"
    >
        <div class="card tm-card p-4">
            <h4 class="tm-card-title mb-3">Messages envoyés</h4>
            <p class="text-muted mb-0">Cette tab affichera l’historique des réponses envoyées.</p>
        </div>
    </div>
</div>