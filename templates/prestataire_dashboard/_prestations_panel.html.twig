<ul class="nav nav-tabs border-bottom gap-2 mb-4" id="prestationsSubTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <button
            class="nav-link active"
            id="prestations-list-tab"
            data-tab-level="sub"
            data-bs-toggle="tab"
            data-bs-target="#prestations-list-panel"
            type="button"
            role="tab"
            aria-controls="prestations-list-panel"
            aria-selected="true"
        >
            Liste des prestations
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button
            class="nav-link"
            id="prestations-pricing-tab"
            data-tab-level="sub"
            data-bs-toggle="tab"
            data-bs-target="#prestations-pricing-panel"
            type="button"
            role="tab"
            aria-controls="prestations-pricing-panel"
            aria-selected="false"
        >
            Tarifs et remises
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button
            class="nav-link"
            id="prestations-visibility-tab"
            data-tab-level="sub"
            data-bs-toggle="tab"
            data-bs-target="#prestations-visibility-panel"
            type="button"
            role="tab"
            aria-controls="prestations-visibility-panel"
            aria-selected="false"
        >
            Visibilité
        </button>
    </li>
</ul>

<div class="tab-content" id="prestationsSubTabsContent">
    <div
        class="tab-pane fade show active"
        id="prestations-list-panel"
        role="tabpanel"
        aria-labelledby="prestations-list-tab"
        tabindex="0"
    >
        <div class="card tm-card p-4">
            <h4 class="tm-card-title mb-3">Liste des prestations</h4>
            <p class="text-muted mb-0">Cette tab affichera l’ensemble des prestations proposées par le prestataire.</p>
        </div>
    </div>

    <div
        class="tab-pane fade"
        id="prestations-pricing-panel"
        role="tabpanel"
        aria-labelledby="prestations-pricing-tab"
        tabindex="0"
    >
        <div class="card tm-card p-4">
            <h4 class="tm-card-title mb-3">Tarifs et remises</h4>
            <p class="text-muted mb-0">Cette tab affichera les prix catalogue, les promotions et les réductions configurées.</p>
        </div>
    </div>

    <div
        class="tab-pane fade"
        id="prestations-visibility-panel"
        role="tabpanel"
        aria-labelledby="prestations-visibility-tab"
        tabindex="0"
    >
        <div class="card tm-card p-4">
            <h4 class="tm-card-title mb-3">Visibilité</h4>
            <p class="text-muted mb-0">Cette tab permettra de gérer la mise en avant et la visibilité des prestations publiées.</p>
        </div>
    </div>
</div>